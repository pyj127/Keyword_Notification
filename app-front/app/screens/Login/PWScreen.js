import React from "react";
import {
  View,
  Text,
  StyleSheet,
  Button,
  TouchableOpacity,
  Image,
  SafeAreaView,
} from "react-native";

const PWScreen = () => {
  return (
    <SafeAreaView>
      <View>
        <Text style={{ fontSize: 40, textAlign: "center" }}>여기는 PW</Text>
      </View>
    </SafeAreaView>
  );
};
export default PWScreen;
